requests>=2.31.0
urllib3>=2.0.0
python-dotenv>=1.0.0
pymupdf>=1.23.0
sentence-transformers>=2.7.0
numpy<2
langdetect>=1.0.9
playwright>=1.40.0
playwright-stealth>=1.0.0
networkx>=3.2
sympy>=1.12
adapters
fastapi
uvicorn
transformers
torch
open_clip_torch>=2.24   # BiomedCLIP image embeddings for Vision-tab correction memory
nltk
ollama
openpyxl>=3.1
python-multipart
Pillow>=10.0
scipy                  # D2IM pre/post-processing (zoom, dilation, gradient)
matplotlib             # D2IM displacement/strain field rendering (Agg backend)
tifffile               # read 8/16-bit micro-CT TIFF slices for the Mechanics tab
tensorflow             # D2IM CNN (Mechanics tab); lazy-imported, optional — the
                       # other four tabs run without it. On Python 3.12 / TF≥2.16
                       # also install tf-keras (below): the weights are a legacy
                       # Keras-2 .h5 that Keras 3 can't load. Pin to a numpy<2
                       # build (e.g. tensorflow==2.16.2) to keep the repo's numpy.
tf-keras               # Keras-2 shim used to load the legacy D2IM .h5 on TF≥2.16
